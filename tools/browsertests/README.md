# Browsertests (Playwright, Chromium)

Draaien tegen een lokale webserver met de inhoud van de repo (`public\` in de OneDrive-map) op poort 8765:

    cd public && python -m http.server 8765
    python demo-flow.py            # startscherm, starterset, autosave, labels, Settings, herbezoek, server, reset, opzoeklinks (42 controles)
    python migratie-en-file.py     # bestaand token zonder serverUrl -> data.php; geen token in de HTML; file:// -> bestandsmodus; data.php ook zonder IndexedDB (neemt het index.html van de repo zelf)
    python opslagbalk.py           # balk bij niet-persistente browseropslag, tekstvarianten (Firefox, iOS, Dock-app), sluiten, sessie (21 controles)
    python formulair-import.py     # DataModel.sqlite inlezen, dezelfde afronding en volgorde als formulair-naar-json.py, de nota over het -wal-bestand, download vergelijken met de referentie (7 controles; MIF_SQLITE zetten)
    python formulair-doorsturen.py # "Add to miFormulas" in een lege browser -> app start met 698 formules (MIF_SQLITE zetten)
    python formulair-datums.py     # de datums van Formulair in de tijd van de computer: een eigen kleine databank met formules van na middernacht, gelezen door beide omzetters in Brussel en in UTC (7 controles; geen eigen databank nodig)
    python merge-import.py         # Formulair-import samengevoegd met bestaande data: naam-matching, diluties, categorieën, overslaan bij tweede import, Undo, lege browser; twee Formulair-materialen met één naam die twee blijven (ook naamloze), een formulenaam die je al hebt als "(Formulair)", en een tweede invoer die niets verdubbelt (33 controles)
    python site-reopen.py          # Open data file… op de site: onthouden, Reopen, Forget in Settings, verdwenen bestand (ook met toestemming granted), Install-knop, Save to a data file… (40 controles)
    python file-mode.py            # gedownloade app via file://: hint, starterset van miformulas.com (nagebootst), databestand aanmaken (45 controles, incl. herstart met Reopen en verdwenen bestand; geen webserver nodig; leest de bouwstempel uit index.html)
    python help.py                 # Help-knop: ingebedde handleiding, inhoudstafel, terug naar de vorige weergave, en de handleiding die elke knop noemt zoals de app hem labelt, weglatingsteken inbegrepen (20 controles)
    python move-into.py            # Move into…: elke formule met één versie als versie van een andere, Move together, "this formula", een eigen formule die niet bevroren binnenkomt, Undo, alleen de verplaatste formules verdwijnen uit de lijst, en prepared en hideSource gaan mee, en een naam in andere hoofdletters in Move together die gevonden wordt (58 controles)
    python materiaallijst.py       # materials library: import, Settings, Get the latest library, + New material met feiten, Browse the library… met shift-klik, "+ Add" in de zoeklijst naast en zonder eigen treffers, een rommelige lijst (dubbels, gedeelde alias, waarden buiten bereik), Undo, accenten en de ligatuur œ die zoeken en opzoeken niet meer in de weg staan, en het plafond van 2 MB met de maat in de melding, en pyramid-woorden in een bibliotheek (76 controles)
    python deel-formule.py        # Share this version: wat er in het bestand komt en wat er niet in komt (solventvlag, label, volgorde, decimalen, lege versie, verweesde regel), en de rondgang bij een ontvanger die het materiaal, het solvent en de formule niet heeft, met de datum van de versie (45 controles)
    python export-bibliotheek.py   # Export my inventory as a library…: alles vooraf aangevinkt, shift-klik, wat er wel en niet in het bestand komt, en de rondgang terug via import (40 controles)
    python import-controle.py      # Import formula… als poort: regel zonder naam, onleesbaar of negatief gewicht, dilutie buiten 0-100 blokkeren Confirm; komma, percentteken en ontbrekende dilutie hersteld; solventvlag; Undo neemt formule, materiaal, bestelregel en categorieën terug; een naam die twee keer nieuw is wordt één materiaal, regels die geen regel zijn worden geweigerd in plaats van te crashen, en een oneindig gewicht komt er niet door; een nieuwe naam op drie regels telt geen treffers, een bestand zonder naam geeft geen bestelregel "needed for undefined" en een bestand zonder regels komt niet door de sluis; twee namen van één bibliotheekingang geven één materiaal, een vorm die geen lijst is zet de app niet vast, en het venster telt wat er werkelijk aankomt; de kop van de voorvertoning noemt de formule zoals ze aankomt; een getal is een getal ("1:10", "2 drops" en "3-4" in het rood), een lijst of een getal waar tekst hoort, korte datums en een datum die niet te lezen is, geen solventwaarschuwing zonder solventvlag, herkomst en regelopmerkingen in de notities en een getrimde materiaalnaam (87 controles)
    python opruiming.py            # staart van de review: zoeken op "starter", bevestiging bij ✕, ellipsis op de openers, aliassen in de keuzelijst, attributie in Settings, alleen-lezen op een telefoon, een lege lijst die uitlegt waarom, een bestelregel die geen knop is, dubbelklik op + New version, de namen van de aankruisvakjes, een materiaal dat alleen in een oudere versie zit, Delete version, de zoekknop die geen winkels belooft die je niet hebt en een gebruikslijst die zegt dat ze afgekapt is; de staart van de vierde audit: Compare vanaf de eerste versie, Close compare terug in de bench, Ctrl+P in Compare, de kolom Cost op de afdruk, de dubbele regels in beide weergaven en Copy met notities en proeflog; de kleine punten van de mini-audit: één Undo-zin in Delete version en de keuzelijsten van Compare die niets over ouder of nieuwer beweren (90 controles)
    python undo-navigatie.py       # één undo-stap per handeling (nieuwe categorie, predilutie, Replace + dilutiewissel, ook door de dilutiedialoog), Redo keert terug naar de plek van de wijziging, een categoriewissel die een categorie aanmaakt, Copy vanuit een oudere versie, bench mee naar een nieuwe versie, een bench-groep die zijn eigen regel vasthoudt en Ctrl+P zonder printknop, + New version en Delete version die je in de bench view laten, en een schikking die herladen en Redo overleeft, met predilutieregels die een id dragen (41 controles)
    python validatie.py            # wat de app weigert: negatief gewicht, dilutie buiten 0-100, dubbele dilutie, basisdilutie die verdwijnt, tekst als getal, eigen velden met underscore, negatief doelgewicht, basisdilutie boven 100 in + New material, Set EtOH zonder ethanol; dagsnapshot, Delete version op een import, verborgen importverwijzing (36 controles)
    python namen.py                # Add line, formule-import en Delivered nemen de feiten uit de bibliotheek mee, categorie niet dubbel door hoofdletters, alias die je al bezit, hernoemen zonder dubbele namen, een alias die meteen meetelt, hernoemen naar andermans alias, dezelfde stof twee keer op dezelfde dilutie, een nieuwe formule die in Uncategorised vertrekt en de kleurkiezer per categorie, die #FFFFFF net als de lijsten als grijs leest; een alias die de eigen naam van een ander materiaal is en daarom nooit iets bereikt, en een bestellijst die dezelfde stof niet twee keer aanvaardt (81 controles)
    python kost-en-ifra.py         # kost per gram zuiver (Delivered met basisdilutie, predilutie met hoeveelheid en IFRA-veld), de kleinste regel in gram in de voorbeschouwing, IFRA 0 = prohibited en negatief = niet nagekeken, de IFRA-dosering op de volledige inhoud met het solvent in dezelfde tabel, een regel naar een gewist materiaal, een kopje dat niets vrijgeeft, Deduct base met een basisdilutie en de laatste dilutie die blijft staan, geen databestand openen, bibliotheek met rommel, server zonder databestand, Delivered dat vraagt of de oude voorraad op is en een negatieve prijs weigert, en de IFRA-tabel die zegt hoeveel beperkte materialen er niet op staan, een dosering 1e999 die geen ∞ achterlaat en de hint over ⇄ die op een oudere versie de weg wijst die er wel is (73 controles)
    python geen-variaties.py       # sinds 260915b geen variaties meer: een oud bestand met variaties laadt met één waarschuwing en houdt het veld, formulepagina, Move into…, Copy, Compare, Welcome-tegel, export en lijstteller kennen alleen versies (15 controles)
    python opslagvolgorde.py       # één schrijfactie tegelijk: wijziging tijdens een trage PUT (nagebootste server in de pagina), geen tweede PUT met dezelfde ETag, browseropslag die weigert, databestand vervangt de browserkopie; dagsnapshot bij onbereikbare server wordt niet over de serverdata geschreven, mislukte eerste schrijfactie naar een bestand; een lege server waar je je eigen Backup binnenbrengt zonder bestandsverwijzing; een pagina die aan de kant stapt (visibilitychange, pagehide) schrijft meteen weg, en bij het sluiten houdt de app het tegen zolang er iets onbewaard is; een notitie die nog de focus heeft telt mee bij sluiten en wegzetten, en in één browser bewaart één venster: een tweede leest alleen, laat een Formulair-import liggen en neemt vanzelf over zodra het eerste sluit, ook vanaf het startscherm (101 controles)
    python getalnotatie.py         # en-GB: duizendtalscheiding in invoervelden, parseNum, Delivered per 1000 g en Amount purchased met een eenheid (ml met density), IFRA-dosering per formule, Ctrl+Z onder een dialoog, Replace + Cancel, Escape op het dilutievenster, gebruiksindex na Add line; de getalvelden van de materiaalpagina, het veldrooster en een spoordilutie die niet als 0 % leest (36 controles)
    python uiterlijk.py           # uitvoer en uiterlijk: CSV met het scheidingsteken en de decimaal van de taal, cellen die met = beginnen, bestandsnaam met een schuine streep, witte achtergrond bij afdrukken, zachte rode kleur in beide thema's, koptekst op één regel bij 1280 px, labels aan hun veld, lijstregels met het toetsenbord, koptekst op 850 px, de kop van een formule, de koptekst op 1440/1280/1024/850 px met tikdoelen en de statusregel, afdrukken uit het donkere thema en een lange koppeling in de handleiding, afdrukken uit Auto op een donker systeem en een weegblad zonder één prijs dat geen kost noemt, en de scheidingstekens van Zwitserland en Japan (61 controles)
    python koptekst-en-focus.py    # de focus blijft in de gewichtskolom (Tab, Shift+Tab, Enter), de bench view past op 1280 px, de telefoon zonder doodlopende wegen (☰, To order), contrast van de amberkleurige tekst in beide thema's, de bench view die niet schrijft in de alleen-lezenstand, --muted dat overal AA haalt, de opslagstand op haar eigen ondergrond, wat alleen-lezen laat staan en een bench die daar niet sleepbaar is, en dezelfde stof op twee diluties die twee paar knoppen geeft met elk hun eigen naam (77 controles)
    python leeg-en-welcome.py      # Start empty op het startscherm, het blok Start here met één primaire knop, geen Recently edited zonder eigen werk, de kolom Cost pas met een prijs, een kaal databestand dat + New formula en + New material niet laat crashen (16 controles)
    python materialen-csv.py       # materialen uit een CSV: de rondgang met de eigen CSV-uitvoer (basis eerst), een vreemd blad met ; en decimale komma en synoniemen als kopnamen, een Windows-1252-bestand, dubbels en rijen zonder naam, het sjabloon, één undo-stap; Get the latest library in het CSV-venster en het Import/Export-venster van 260916d; aliassen die aanvullen in plaats van wissen, negatieve prijs en dichtheid, een BOM bij de terugval naar windows-1252, een formulesblad dat geweigerd wordt, lege uitvoer die dat zegt en een lege Solvent-cel die de bibliotheek laat staan, dezelfde dilutie twee keer in één cel, en een plan dat de aliassen meetelt die de invoer zelf meegeeft, en een aankoopdatum in de korte vorm van een spreadsheet (86 controles)
    python formules-uitvoer.py     # alle formules naar één bestand en weer terug: de uitvoer draagt versies, labels en notities maar geen labogegevens en geen materialId, de voorvertoning vat samen, de materialen komen als "to order" zonder de bestellijst te vullen, alles in één undo-stap, een tweede invoer geeft "(import)" en een onleesbare regel blokkeert Confirm, en een regel naar een gewist materiaal valt uit beide Excel-uitvoeren en wordt ook vóór de grote JSON-uitvoer gevraagd, en een versie zonder datum houdt haar lege datum bij de rondgang (47 controles)
    python formules-csv.py         # formules uit een CSV: de rondgang van Export all formulas (Excel) met de elfde kolom Notes en zes decimalen voor een spoorregel, een blad zonder Formula-kolom (bestandsnaam als formule, "v3 45gr" wordt het label), een vreemd blad met ; en decimale komma en synoniemen, de Total-regel als gewichtscontrole, een onleesbaar gewicht dat Confirm blokkeert, een materialenblad dat geweigerd wordt, een Total-regel met een dilutie, een label dat niet doorloopt en de datum uit het blad, en een blad zonder Date-kolom dat vandaag krijgt terwijl een lege Date-cel leeg blijft; datums zoals Excel ze terugschrijft, de Excel-uitvoer van één versie die terugleest als nieuwe versie, en twee formules met één naam die niet versmelten (82 controles)
    python terug-en-vooruit.py     # terug en vooruit via de geschiedenis van de browser: elke plaats is een stap, de versie komt mee, de handleiding telt mee, een verwijderd item wordt overgeslagen, een herlaadbeurt landt op dezelfde plek, de geïnstalleerde app toont twee knoppen die aan de uiteinden uitgaan en ook wanneer de browser geen stappen meer heeft, van versie wisselen is zelf een stap, en aan het einde van de geschiedenis draagt de ingang dezelfde teller als de app, ook na een herlaadbeurt (36 controles; Playwright kan de zijknoppen van een muis niet sturen, go_back/go_forward doen hetzelfde)
    python lijst-en-navigatie.py   # de zoekterm hoort bij de lijst (Formulas, Materials, To order) en blijft niet staan bij het wisselen; een ander item begint bovenaan in het paneel, terwijl een re-render van dezelfde pagina de leespositie houdt, ook in de handleiding, en zoeken zonder treffer in To order (27 controles)
    python instellingen.py         # Settings: de servervelden staan er op de site en niet in de gedownloade app (met de hint die de weg wijst), en Open where you left off onthoudt tabblad, formule of materiaal en versie per toestel, herstelt ze bij de start, slaat een verdwenen formule over en wist alles zodra je het uitzet (37 controles)
    python robuustheid.py          # wat de app moet overleven: databestand met gaten en dubbele id's, een host die elke url met html beantwoordt, een onleesbare browserkopie, tekst in een getalveld; Enter in een dialoog (één keer, ook na vijf openingen), Tab vanaf het startscherm (inert), klikbare elementen met het toetsenbord, Undo terug naar de wijziging, aliases als lijst (de vorm van het bibliotheekformaat) die één tekst met kommapunten wordt, en een getal of een lijst waar tekst hoort in het databestand zelf (55 controles)
    python safari-hints.py         # Safari op de Mac: Add to Dock-link en dialoog, waarschuwing bij Download the app, alleen-lezen startscherm via file://, opslagbalk in tabblad en Dock-app, Import from Formulair op de Welcome-pagina, importer in de Dock-app, dagsnapshot op het alleen-lezen startscherm (40 controles)
    python server-drop.py          # een 409 die je eigen schrijfbeurt is: het antwoord op een PUT gaat verloren en Chromium stuurt ze opnieuw, of een schrijfbeurt die als mislukt gold kwam toch aan; en een echt conflict blijft een conflict. Tegen de echte server/worker.js achter een eigen node-server (worker-server.mjs), geen webserver op 8765 nodig (12 controles)
    python webkit.py               # WebKit (de motor van Safari) naast Chromium: rekenkern, getalnotatie, vouwing en volgorde, de uitvoer, elke stijlregel, IndexedDB en een herlaadbeurt (25 controles; eigen pakket, zie onder)
    python screenshots.py          # geen test: maakt de app-schermen voor docs/img (zie docstring)

`python tools/controleer-bench.js <app.html> <data.json>` zegt of de bench-schikkingen van een databestand de overstap van bouw 260918d overleven (het verandert niets).

`webkit.py` heeft een eigen motor nodig, want Playwright levert er hier geen: `sudo apt-get install -y
webkit2gtk-driver xvfb && pip install selenium`. Zonder die twee stopt het script met exitcode 2 en zegt
het wat er ontbreekt, zodat het in een reeks geen valse fout geeft. Het is WebKitGTK, niet Safari: wat het
niet kan zien (de opruiming van IndexedDB na zeven dagen, Add to Dock, iOS, de printmotor van Safari)
staat onderaan in dat bestand en blijft werk voor een echte Mac of iPhone.

Vereist `pip install playwright` en `playwright install chromium`. De scripts zoeken de app en de
starterset zelf op, twee mappen hoger; alleen de twee Formulair-tests hebben je eigen database nodig,
want die staat niet in de repo:

    MIF_SQLITE=/pad/naar/DataModel.sqlite python formulair-import.py

`formulair-import.py` maakt de Python-referentie zelf aan met `formulair-naar-json.py` als ze er nog
niet is, en laat ze met wat de browser maakte naast zich staan (`MIF_REF` en `MIF_OUT` wijzen elders).
